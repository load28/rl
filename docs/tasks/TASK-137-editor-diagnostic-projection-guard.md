# TASK-137: 에디터 타입 진단 투영 가드

- **상태**: 완료
- **시작일**: 2026-08-21
- **완료일**: 2026-08-21
- **커밋**: —

## 목적

TypeScript 진단 코드 번호로 파일 전체 진단을 버리는 휴리스틱을 제거하고, rlc가 방출한 문서가 실제로 파싱 가능한지에 따라 투영 가능성을 판정한다.

## 범위

- 포함: `service_diagnostics`의 진단 신뢰성 가드와 회귀 테스트
- 제외: 진단 병합·번역 규칙, 프로젝트 Snapshot 부분 실패

## 의사결정

### 결정 1: 진단 번호가 아니라 출력 파싱 결과를 사용한다

- **상황**: 기존 `code < 2000` 조건은 TS1117처럼 파싱 가능한 출력의 진단도 파일 전체 블랙아웃으로 처리한다.
- **검토한 대안**: 예외 코드 목록은 TypeScript 진단 체계에 계속 결합된다. 가드를 완전히 제거하면 파싱 불가능한 출력에서 checker 회복 진단을 사용자에게 노출한다.
- **선택과 근거**: 이미 compiler boundary에서 사용하는 `verify_output`으로 서비스 문서 자체를 검사한다. 출력이 TypeScript로 파싱될 때만 서비스 진단을 매핑한다.

## 작업 내역

- 2026-08-21: TASK-137을 진행 중으로 등록했다.
- 2026-08-21: `src/engine/language.rs`에서 `< 2000` 진단 코드 필터를 제거하고 `verify_output` 기반 가드를 추가했다.
- 2026-08-21: 중복 객체 키처럼 파싱 가능한 1000번대 진단 후보는 허용하고, 파싱 불가능한 출력은 거부하는 단위 테스트를 추가했다.
- 2026-08-21: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`를 실행했다.

## 이슈 및 해결

없음.

## 검증

- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo test`

## 결과

에디터 진단의 파일 단위 억제 기준이 TypeScript 진단 번호에서 실제 방출 문서의 파싱 가능성으로 바뀌었다. 전체 검증 게이트가 통과했다.
